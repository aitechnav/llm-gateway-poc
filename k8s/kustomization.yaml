apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-gateway-poc

resources:
  - namespace.yaml
  - secrets.example.yaml
  - sentinelguard-config.yaml
  - mock-llm.yaml
  - sentinelguard.yaml
  - chatbot.yaml
  - alertmanager.yaml
  - prometheus.yaml
  - grafana.yaml
